import os
import re
from pathlib import Path

ROOT = Path("/home/infiniteceo/projects/cheza/mavunoguard-ai-hackathon")
SERVER = ROOT / "server"

# We will read the original app.py and extract parts manually or just rewrite them cleanly.
# Actually, since I have the code context, I can just write the content of each file.
